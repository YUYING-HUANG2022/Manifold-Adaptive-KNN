# Manifold-Adaptive-KNN
# This repository created two strategies of searching a locally-consistent region with high proximity to the test instance, on which global manifold correction is applied to maintain global generalisation. 
